<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:wpfui="http://schemas.lepo.co/wpfui/2022/xaml"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:viewModels="clr-namespace:Desktop.ViewModels"
                    xmlns:commands="clr-namespace:Desktop.Commands">

    <DataTemplate DataType="{x:Type viewModels:Tracker}">

        <wpfui:Card HorizontalAlignment="Right" VerticalAlignment="Bottom" HorizontalContentAlignment="Right">
            
            <UniformGrid Columns="1">
                <Border Common.IsVisible="{Binding Config.ShowBluetoothDeviceBattery}">
                    <ContentPresenter Content="{Binding BluetoothBattery}"/>
                </Border>
                <Border Common.IsVisible="{Binding Config.ShowCpuUsage}">
                    <ContentPresenter Content="{Binding CpuUsage}"/>
                </Border>
                <Border Common.IsVisible="{Binding Config.ShowCpuTemperature}">
                    <ContentPresenter Content="{Binding CpuTemperature}"/>
                </Border>
                <Border Common.IsVisible="{Binding Config.ShowMemory}">
                    <ContentPresenter Content="{Binding Memory}"/>
                </Border>
            </UniformGrid>
            
        </wpfui:Card>

    </DataTemplate>
    
</ResourceDictionary>
